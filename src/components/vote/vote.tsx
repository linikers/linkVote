import { Typography } from "@material-ui/core"
import { FC } from "react"

export const Vote: FC = () => {

    return(
        <form>
        <Typography>Vote Agora</Typography>
        </form>
    )
}